name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.18'

      - name: Install dependencies
        run: go mod tidy
        working-directory: ./  # Set to the directory where go.mod is located

      - name: Run tests
        run: go test ./... -v
        working-directory: ./  # Set to the directory where go.mod is located

      - name: Lint the code
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
          golangci-lint run
        working-directory: ./  # Set to the directory where go.mod is located

      - name: Deploy to Staging
        if: success()
        run: |
          echo "Deploying to staging environment..."
        working-directory: ./  # Set to the directory where go.mod is located